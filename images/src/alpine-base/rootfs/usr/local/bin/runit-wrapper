#!/bin/sh

PID=

onterm() {
    if [ -n "${PID}" ]; then
        kill -SIGHUP "${PID}"
    fi
}

trap onterm SIGTERM SIGINT SIGHUP
/sbin/runsvdir -P /etc/service "log: ..........................................................................................................................................................................................................................................................................................................................................................................................................." &
PID=$!
wait "${PID}"
