#!/bin/sh

PID=

onterm() {
    if [ -n "${PID}" ]; then
        kill -HUP "${PID}"
    fi
}

trap onterm TERM INT HUP
/bin/runsvdir -P /etc/service "log: ..........................................................................................................................................................................................................................................................................................................................................................................................................." &
PID=$!
wait "${PID}"
